function squareOf() {
    return num * num;
}

// Se recomienda usar const para llamar funciones con varibales

const squareOf = (num) => {
    return num * num;
} 

// Se puede realizar un return implicito de la siguiente manera

const squareOf = num => num * num;
