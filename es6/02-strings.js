// Antes de ES6 se concatenamaban los strings así

let hello = "Hello";
let world = "World";
let phrase = hello + " " + world;
console.log(phrase);

// Template literals
let phrase2 = `${hello} ${world}!`
console.log(phrase2);